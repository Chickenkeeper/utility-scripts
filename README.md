Scripts:

| Name | Description |
| --- | --- |
| `application-launcher-refresh` | refreshes the cache of the KDE application launcher |
| `archive-image-sequence` | converts a sequence of images to an ffv1 video |
| `backup` | backs up a list of directories to another directory |
| `blender-update` | installs/updates blender from a file |
| `clamscan-full-scan` | performs a virus scan on the whole system using clamscan |
| `clear-thumbnail-cache` | clears the thumbnail cache of a system running KDE Plasma |
| `davinci-resolve-install` | assists with installing/updating Davinci Resolve from a file |
| `kde-powerdevil-restart` | restarts KDE PowerDevil in case it breaks when a monitor is reconnected |
| `odin-update` | installs/updates the Odin programming language from a file |
| `ols-update` | installs/updates the Odin Language Server |
