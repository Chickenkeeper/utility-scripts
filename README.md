Scripts:

| Name                               | Description                                                             |
| ---------------------------------- | ----------------------------------------------------------------------- |
| `backup`                           | Backs up a list of directories to a borgbackup archive                  |
| `blender-install`                  | Installs/updates Blender from a file                                    |
| `clamav-full-scan`                 | Performs a virus scan across the whole system using clamscan            |
| `clear-thumbnail-cache`            | Clears the thumbnail cache of a system running KDE Plasma               |
| `davinci-resolve-install`          | Assists with installing/updating Davinci Resolve                        |
| `image-sequence-to-ffv1`           | Creates an FFV1 video from an image sequence                            |
| `kde-application-launcher-refresh` | Refreshes the cache of the KDE application launcher                     |
| `kde-powerdevil-restart`           | Restarts KDE PowerDevil in case it breaks when a monitor is reconnected |
| `odin-lang-install`                | Installs/updates the Odin programming language from a file              |
| `ols-install`                      | Downloads and installs/updates the Odin Language Server                 |
| `tinymist-install`                 | Downloads and installs/updates the Tinymist language server for Typst   |
| `typst-install`                    | Downloads and installs/updates the Typst typsetting system              |
